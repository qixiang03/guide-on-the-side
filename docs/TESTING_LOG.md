# Project Testing Log - UPEI Guide-on-the-Side

This document maintains a history of development, peer reviews, and functional testing for the Pressbooks tutorial project.

---

| Entry | Component                           | Date           | Author  | Reviewer |
| ----- | ----------------------------------- | -------------- | ------- | -------- |
| 001   | CI Infrastructure                   | 2026-02-09     | Xiaohan |          |
| 002   | CI Auto Stub                        | 2026-02-22     | Xiaohan |          |
| 003   | Steps Data Normalization Unit Tests | 2026-02-22     | Xiaohan | Yang     |
| 004   | Integration Smoke Tests             | 2026-03-02     | Xiaohan | Caleb    |
| 005   | Analytics Subsystem Unit Tests      | 2026-03-04     | Enzo    | Xiaohan  |
| 006   | README Refactor, Analytics Tracking & Code Cleanup | 2026-03-09 | Enzo | Xiaohan |
| 007   | Certificate, H5P AJAX, Dashboard & JS Tests, Black-Box TC-11–TC-13 | 2026-03-10 |Xiaohan | Yang|
| 008   | Template Picker & Export/Import Test Coverage | 2026-03-23 | Xiaohan | Daniel |
| 009   | Merge main→develop: Test Fixes for RBAC + Template Picker + Upload Stubs | 2026-03-24 | Enzo | Xiaohan|
| 010   | Accessibility Dashboard, Custom Themes, Keyboard Shortcuts & ARIA Labels | 2026-03-29 | Enzo  |Xiaohan |
| 011   | Branch Sub-Tutorial Frontend Restoration (Cindy's lost merge code) | 2026-03-29 | Enzo  |Yang |
| 012   | pb-split-guide Test Gap Closure — Template/Export/H5P AJAX/A11y + Smoke Hook Strategy | 2026-03-30 | Xiaohan |Yang |
| 013   | pb-split-guide Specific Confirmation Dialogs (delete step, leave page, librarian deactivate/reactivate) | 2026-03-30 | Xiaohan |Yang |
| 014   | Analytics Audit Fixes — Branch Quiz Exclusion, Draft Filtering, Device Detection, Normalizer Bug | 2026-04-06 | Enzo |Xiaohan |
| 015   | PBSG_Certificate — mark_completed always latest timestamp + unit test alignment | 2026-04-06 | Xiaohan | Yang|
| 016   | H5P Ownership, Template Duplication, Inline Name Editing + Benchmark Sliders + CSV Export Fix | 2026-04-09 | Enzo |Xiaohan|
| 017   | Tutorial Intro Layout, Branch H5P Conversion, Quiz Typography Refresh, H5P Edit Bug Fix | 2026-04-12 | Enzo | Xiaohan|
| 018   | Network Admin Menu Visibility Fix + Admin Guide + Dev Setup Guide Update | 2026-04-13 | Enzo |Xiaohan |
| 019   | Nav Wrap + Summary Redesign + Toolbar/Dropdown Polish + Google Docs Viewer Fix + composer.lock + CI/CD Doc | 2026-04-19 | Enzo |  |


---

## **Log Entry: 001**

### **1. Component Information**

- Component Name: Automated Testing Infrastructure & Lightweight CI/CD Pipeline
- History (Date): February 9, 2026
- Generated By (Code Developer): Xiaohan Yu (Reagan)
- Reviewed By (Independent Reviewer):
  _Peer review must be conducted by someone other than the developer._

**Traceability**

- Commit SHA: c3331ae
- Branch: testing-ci-setup → main

### **2. Black-Box Tests Performed**

_(Functional verification from a user/system perspective. No internal implementation details.)_

1. **CI Workflow Trigger**

- Input/Action: Push changes to `testing-ci-setup` and open a PR to `main`.
- Expected Result: GitHub Actions runs automatically for the PR.
- Actual Result: PASS

2. **Environment Configuration & Dependency Installation**

- Input/Action: Run CI using `.github/workflows/ci.yml`.
- Expected Result: PHP version(s) configured in CI start successfully and dependencies install without conflicts.
- Actual Result: PASS

3. **Automated Test Execution & Report Generation**

- Input/Action: Execute `composer test` in CI.
- Expected Result: PHPUnit completes and generates a JUnit XML report; report is uploaded as an artifact.
- Actual Result: PASS

4. **System Smoke Test (Minimal)**

- Input/Action: Start the Pressbooks environment (Lando) and load the plugin/page related to the change.
- Expected Result: No fatal error; page renders; expected UI elements load.
- Actual Result: PASS

### **3. Verification Evidence**

- CI Execution Run: https://github.com/qixiang03/guide-on-the-side/actions/runs/21838680073
- Artifacts (Actions → Run → Artifacts):
  - Artifact Name: `phpunit-test-log`
  - Contents/Path: `build/junit.xml` (JUnit XML test report)

  ***

## **Log Entry: 002**

### **1. Component Information**

- Component Name: CI Testing Log Auto-Stub Integration
- Date: February 22, 2026
- Generated By: Xiaohan Yu (Reagan)
- Reviewed By:
  _Peer review must be conducted by someone other than the developer._

**Traceability**

- Commit SHA: 28fb28d
- Branch: testing-ci-setup → main

### **2. Black-Box Tests Performed**

1. **Automatic Stub Generation**

- Input/Action: Open a pull request targeting `main`.
- Expected Result: CI completes and automatically posts a testing-log stub comment in the PR.
- Actual Result: PASS

2. **CI Evidence Validation**

- Input/Action: Navigate to the CI run page from the PR.
- Expected Result: The run page contains a downloadable artifact (`phpunit-test-log`) with `build/logs/junit.xml`.
- Actual Result: PASS

### **3. Verification Evidence**

- CI Execution Run: https://github.com/qixiang03/guide-on-the-side/actions/runs/22287486178
- Artifacts (Actions → Run → Artifacts):
  - Artifact Name: `phpunit-test-log`
  - Contents/Path: `build/logs/junit.xml`

---

## **Log Entry: 003**

### **1. Component Information**

- Component Name: Steps Data Normalization Unit Tests
- Date: 2026-02-22
- Generated By: Xiaohan
- Reviewed By: Yang
  
**Traceability**

- Commit SHA: 14db22f
- Branch: testing-ci-setup → main

### **2. Black-Box Tests Performed**

1. Input: Legacy step with "url" only.
-   Expected: Automatically migrated to tutorial_type = "url".
-   Result: PASS

2. Input: Invalid tutorial_type value.
-   Expected: Normalized to "file" if attachment_id present.
-   Result: PASS

3. Input: Completely empty step row.
-   Expected: Skipped during normalization.
-   Result: PASS

### **3. Verification Evidence**

- CI Run: https://github.com/qixiang03/guide-on-the-side/actions/runs/22289489877
- Artifact: phpunit-test-log
- JUnit Path: build/logs/junit.xml

---

## **Log Entry: 004**

### **1. Component Information**

- Component Name: Integration Smoke Tests (Plugin ↔ WordPress Wiring)
- Date: 2026-03-02
- Generated By: Xiaohan
- Reviewed By: Caleb

**Traceability**

- Commit SHA: 927b636
- Branch: testing-ci-setup → main

**Approach:** Lightweight WordPress function stubs that record hook registrations and
function calls, allowing verification of plugin ↔ WP integration without a real
WordPress runtime or database. H5P internals are explicitly excluded.

### **2. Black-Box Tests Performed**

1. Hook Registration (7 tests)
   - Input: Instantiate `PB_Split_Guide_Plugin`.
   - Expected: All 2 filters and 5 actions registered with correct WordPress hook names.
   - Result: PASS

2. Template Registration (2 tests)
   - Input: Call `register_page_template()` with empty and pre-existing template arrays.
   - Expected: `split-guide-template.php` key added, existing templates preserved.
   - Result: PASS

3. Template Loading (3 tests)
   - Input: Call `load_page_template()` under matching, non-page, and wrong-template conditions.
   - Expected: Returns plugin template path when matched; passes through original otherwise.
   - Result: PASS

4. Meta Save Guards (5 tests)
   - Input: Call `save_meta()` with missing nonce, invalid nonce, autosave, insufficient capability, and valid conditions.
   - Expected: `update_post_meta` only called when all guards pass; normalizer applied to steps data.
   - Result: PASS

5. Asset Enqueue — Frontend (3 tests)
   - Input: Call `enqueue_assets()` when not a page, wrong template, and correct template.
   - Expected: CSS only enqueued when template matches.
   - Result: PASS

6. Asset Enqueue — Admin (3 tests)
   - Input: Call `enqueue_admin_assets()` for non-editor hooks, wrong post type, and page editor.
   - Expected: Scripts/styles/media/thickbox/localization only loaded on page editor.
   - Result: PASS

7. AJAX Handler (1 test)
   - Input: Call `ajax_list_h5p()`.
   - Expected: `check_ajax_referer` called with `pbsg_h5p_picker` before any DB access.
   - Result: PASS

### **3. Verification Evidence**

- Local Run: 30 tests, 69 assertions, 0 failures (PHPUnit 9.6.34, PHP 8.3)
- CI Run: https://github.com/qixiang03/guide-on-the-side/actions/runs/22629367601
- Artifact: phpunit-test-log
- JUnit Path: build/logs/junit.xml

---

## **Log Entry: 005**

### **1. Component Information**

- Component Name: Analytics Subsystem Unit Tests
- Date: 2026-03-04
- Generated By: Enzo (Qi Xiang Phang)
- Reviewed By: Xiaohan

**Traceability**

- Commit SHA: 950572f
- Branch: develop → main
- Test Files:
  - `tests/Unit/PBSGAnalyticsSchemaTest.php` (17 tests)
  - `tests/Unit/PBSGAnalyticsEventIngestionTest.php` (31 tests)
  - `tests/Unit/PBSGAnalyticsAggregationTest.php` (23 tests)
  - `tests/Unit/PBSGAnalyticsPrivacyTest.php` (13 tests)
  - `tests/Unit/PBSGAnalyticsDashboardApiTest.php` (30 tests)
  - `tests/Unit/PBSGAnalyticsCsvExportTest.php` (16 tests)
  - `tests/Unit/PBSGAnalyticsEdgeCaseTest.php` (31 tests)
  - `tests/Unit/Helpers/MockWpdb.php` (test helper)

**Approach:** Lightweight WordPress function stubs (WPStubs) and MockWpdb that record
function calls and SQL queries without a real WordPress runtime. Private/static methods
tested via ReflectionMethod. Features requiring real WP infrastructure (dbDelta, header/exit)
verified via source-code inspection. WPDieException simulates `die()` behavior in
`wp_send_json_*` and `wp_die` stubs.

### **2. Black-Box Tests Performed**

1. Schema & Migration (17 tests)
   - Input: Inspect `PBSG_Analytics` source for CREATE TABLE statements.
   - Expected: 3 tables (`pbsg_tutorial_stats`, `pbsg_question_stats`, `pbsg_daily_stats`) with correct columns, types, unique keys, indexes, charset collation, and `$wpdb->prefix` usage.
   - Result: PASS

2. Event Ingestion (31 tests)
   - Input: Call `handle_track_event()` with various HTTP methods, payloads, rate-limit states, and event types.
   - Expected: GET/PUT rejected (405), empty payload rejected (400), rate-limited requests blocked, valid events dispatched to correct recorder methods. 6 event types validated: `tutorial_view`, `tutorial_complete`, `slide_view`, `quiz_attempt`, `quiz_giveup`, `session_flush`. AJAX hooks registered for both `nopriv` and authenticated users.
   - Result: PASS

3. Aggregation Logic (23 tests)
   - Input: Invoke private recorder methods (`record_tutorial_view`, `record_tutorial_complete`, `record_quiz_attempt`, `record_quiz_giveup`, `record_session_flush`, `record_slide_view`) with various data payloads.
   - Expected: SQL uses `INSERT ... ON DUPLICATE KEY UPDATE` upsert pattern. Counters increment correctly (`total_views = total_views + 1`). Time values accumulate. Daily stats bucketed by date. Device type included in queries. Step dwell times merged as JSON. `format_time()` produces correct `Xm YYs` output.
   - Result: PASS

4. Privacy Compliance — PIPEDA (13 tests)
   - Input: Trigger rate limiting, tutorial view recording, quiz attempt recording, session flush with known IP addresses and user-agent strings.
   - Expected: Rate-limit transient keys use MD5 hash prefix (not raw IP). No IP address, user_id, session_id, cookie, username, email, or user_agent appears in any SQL query. CREATE TABLE schemas contain no PII columns. Device detection uses only `HTTP_USER_AGENT` header. Missing `REMOTE_ADDR` falls back to `'unknown'`. Rate-limit TTL is 60 seconds.
   - Result: PASS

5. Dashboard Data API (30 tests)
   - Input: Call `handle_get_analytics()` with unauthorized user, various view types (overview, tutorial, question, compare, unknown), date ranges, and tutorial IDs.
   - Expected: Unauthorized users receive 403 via `wp_die`. Overview returns `totals`, `tutorials`, `daily_trend`, `device_breakdown`, `date_scope`. Tutorial detail returns `stats`, `daily_views`, `step_dwell`, `questions`, `giveup_rate`. Question detail returns `attempt_distribution`, `correct_rate`, `all_time` scope. Date inputs sanitized against SQL injection and XSS. Dashboard hooks register on `admin_menu`. Assets conditionally enqueued only on analytics page.
   - Result: PASS

6. CSV Export (16 tests)
   - Input: Call `handle_export_csv()` without admin capability; inspect source for CSV structure.
   - Expected: Unauthorized users rejected with `wp_die('Unauthorized')`. Source verifies `edit_pages` capability. 4 export types supported (`overview`, `questions`, `question_detail`, `compare`). Correct CSV headers for each type. Content-Type, Content-Disposition, and Cache-Control headers set. Uses `fputcsv()` for proper escaping. Writes to `php://output`. Default export type is `overview`.
   - Result: PASS

7. Edge Cases & Regression (31 tests)
   - Input: Zero-tutorial states, empty tutorial lists, comparison with empty/zero/non-numeric IDs, question text exceeding 500 chars, `absint()` with negative/float/string/non-numeric values, zero and missing H5P content IDs, large time values (9000s), negative time input, rate-limit boundary (59 vs 60 requests), missing/empty step dwell times, zero-attempt division safety, negative `third_plus_correct` clamping.
   - Expected: All edge cases handled gracefully without errors or data corruption. Question text truncated at 500 chars. `absint()` returns 0 for invalid inputs. `format_time()` handles boundary values correctly. Rate limit blocks at exactly 60 requests. Division by zero avoided in percentage calculations.
   - Result: PASS

### **3. Verification Evidence**

- Local Run: 206 tests (181 new + 25 existing), 418 assertions, 0 failures (PHPUnit 9.6.34, PHP 8.3, 0.105s)
- Test command: `docker exec -w /app osspblocal_appserver_1 ./vendor/bin/phpunit`
- CI Run: https://github.com/qixiang03/guide-on-the-side/actions/runs/22691597832
- Commit SHA: 950572f
- Artifact: phpunit-test-log
- JUnit Path: build/logs/junit.xml

---

## **Log Entry: 006**

### **1. Component Information**

- Component Name: README Refactor, Analytics Step Tracking Improvements & Code Cleanup
- Date: 2026-03-09
- Generated By: Enzo (Qi Xiang Phang)
- Reviewed By: Xiaohan

**Traceability**

- Commit SHA: d969ca6
- Branch: develop → main
- Changed Files:
  - `README.md` (documentation rewrite)
  - `web/app/plugins/pb-split-guide/assets/split-guide-tracker.js` (step tracking logic)
  - `web/app/plugins/pb-split-guide/class-pbsg-analytics.php` (backend analytics)
  - `web/app/plugins/pb-split-guide/assets/analytics-dashboard.js` (step name display)
  - `web/app/plugins/pb-split-guide/pb-split-guide.php` (legacy code removal)
  - `web/app/plugins/pb-split-guide/assets/admin/admin-split-guide.css` (stale CSS cleanup)
  - `web/app/plugins/pb-split-guide/assets/split-guide.css` (stale CSS cleanup)
  - `tests/Unit/PBSGAnalyticsAggregationTest.php` (removed `format_time` tests)
  - `tests/Unit/PBSGAnalyticsEdgeCaseTest.php` (removed `format_time` edge case tests)
  - `tests/Integration/Helpers/WPStubs.php` (stub updates)
  - `tests/Unit/Helpers/MockWpdb.php` (mock insert method)

**Approach:** Refactored analytics step tracking to use MutationObserver on actual DOM
elements instead of unreliable `hashchange` events. Removed deprecated `format_time()`
server-side utility (now client-side). Cleaned up legacy code and stale CSS rules.

### **2. Black-Box Tests Performed**

1. **Step Tracking — MutationObserver Detection**
   - Input/Action: Navigate between steps in a split-guide tutorial using Prev/Next buttons.
   - Expected: `split-guide-tracker.js` detects step changes via MutationObserver on `#pbsgProgress` element, matching both `X / Y` and `X of Y` progress text formats.
   - Result: PASS (local verification)

2. **Step Tracking — Button Click Interception**
   - Input/Action: Click `#pbsgPrev`, `#pbsgNext`, and `.pbsg-menu-item` elements.
   - Expected: Tracker intercepts clicks on correct DOM IDs (not legacy `.split-guide-prev`/`.split-guide-next` class selectors).
   - Result: PASS (local verification)

3. **Analytics Backend — Device-Agnostic Daily Stats**
   - Input/Action: Record a slide view when no daily stats row exists for the current day.
   - Expected: `record_slide_view()` fetches the row with highest `view_count` for the current day/tutorial (device-agnostic), and inserts a fallback row if none exists.
   - Result: PASS

4. **Analytics Backend — Step Names in Tutorial Detail**
   - Input/Action: Call `get_tutorial_detail()` for a tutorial with named steps in `_pbsg_steps_json`.
   - Expected: Response includes `step_names` array with human-readable step labels from post meta.
   - Result: PASS

5. **Deprecated Code Removal — format_time()**
   - Input/Action: Run full PHPUnit suite after removing `format_time()` from `PBSG_Analytics` and its associated tests.
   - Expected: All remaining tests pass. No references to `format_time()` in test files.
   - Result: PASS (195 tests, 407 assertions, 0 failures)

6. **Legacy Code Cleanup**
   - Input/Action: Verify `pb-split-guide.php` no longer contains commented-out step-saving code, orphan meta constants, or unused role/permission methods.
   - Expected: Plugin file contains only active, used code. No stale CSS rules in asset files.
   - Result: PASS

### **3. Verification Evidence**

- Local Run: 195 tests, 407 assertions, 0 failures (PHPUnit 9.6.34, PHP 8.3, 0.145s)
- Test command: `lando phpunit --configuration phpunit.xml --testdox`
- CI Run: https://github.com/qixiang03/guide-on-the-side/actions/runs/22822834296
- Commit SHA: d969ca6
- Artifact: phpunit-test-log
- JUnit Path: build/logs/junit.xml
- CI Status: FAIL (3 integration smoke test failures due to git auto-merge re-introducing removed code from `main` — see item 7 above)

---

## **Log Entry: 007**

### **1. Component Information**

- Component Name: Certificate, H5P AJAX, Dashboard Render & JS Tests; Black-Box TC-11–TC-13
- Date: 2026-03-10
- Generated By: Xiaohan
- Reviewed By: Yang

**Traceability**

- Commit SHA: 6e8d7cf
- Branch: main 
- Test / doc changes:
  - New: `tests/Unit/PBSGCertificateTest.php`, `tests/Unit/PBSGH5PAjaxTest.php`, `tests/Integration/Helpers/MockTCPDF.php`, `tests/js/` (Jest: admin-step-utils, split-guide-tracker), `package.json`
  - Modified: `tests/Integration/Helpers/WPStubs.php` (user stubs, nocache_headers, esc_html__), `tests/Unit/Helpers/MockWpdb.php` (get_var), `tests/bootstrap.php` (MockTCPDF), `tests/Integration/PBSplitGuidePluginSmokeTest.php` (Certificate init, activation hook), `tests/Unit/PBSGAnalyticsDashboardApiTest.php` (render_dashboard), `tests/black-box-test-cases.md` (TC-11–TC-13), `tests/regression-checklist.md` (Analytics & Certificate section)

**Approach:** Unit tests for PBSG_Certificate (ajax_mark_completed, ajax_download_certificate) and PB_Split_Guide_Plugin::ajax_list_h5p with MockWpdb; smoke tests for Certificate init and register_activation_hook; render_dashboard $_GET sanitization and data attributes; Jest tests for admin pure functions and tracker tutorial_view payload; black-box cases for Analytics Dashboard, certificate completion, and PDF download; regression checklist updated.

### **2. Black-Box Tests Performed**

1. **Certificate — auth, nonce, tutorial validation (11 tests)**
   - Input: ajax_mark_completed / ajax_download_certificate with not logged in, bad nonce, invalid tutorial_id, wrong template, not completed.
   - Expected: 401/403/400 and wp_send_json_error or wp_die as appropriate; valid flow updates user meta and returns success.
   - Result: PASS

2. **H5P AJAX — table existence and items (4 tests)**
   - Input: ajax_list_h5p with table missing vs present, empty rows; referer check.
   - Expected: wp_send_json_error when table missing; wp_send_json_success with correct items structure; check_ajax_referer called.
   - Result: PASS

3. **Smoke — Certificate init and activation hook (2 tests)**
   - Input: PBSG_Certificate::init(); plugin load in separate process.
   - Expected: wp_ajax_pbsg_mark_completed and wp_ajax_pbsg_download_certificate registered; register_activation_hook called with PBSG_Analytics::create_tables.
   - Result: PASS

4. **Dashboard render_dashboard — $_GET and data attributes (6 tests)**
   - Input: render_dashboard() with various $_GET (tab, tutorial_id, h5p_id, q_index).
   - Expected: data-view, data-tutorial-id, data-h5p-id, data-q-index in output; tab/tutorial_id absint and sanitized.
   - Result: PASS

5. **JS — admin utils and tracker (15 tests)**
   - Input: Jest tests for escapeHtml, safeParseJson, normalizeStep, tutorialSummary; tracker load with mocked fetch and pbsgTracker.
   - Expected: Pure functions match spec; tutorial_view event sent with correct payload and tutorial_page_id.
   - Result: PASS

6. **Black-box and regression docs**
   - Input: TC-11 (Analytics Dashboard), TC-12 (Certificate completion), TC-13 (PDF download) in black-box-test-cases.md; Analytics & Certificate section in regression-checklist.md.
   - Expected: Manual regression can be run against these cases.
   - Result: DOC UPDATED

### **3. Verification Evidence**

- PHP: 218 tests, 471 assertions, 0 failures (PHPUnit 9.6.34) — e.g. `lando phpunit --configuration phpunit.xml --testdox`
- JS: 15 tests, 2 suites (Jest) — e.g. `npm run test:js`
- CI Run: https://github.com/qixiang03/guide-on-the-side/actions/runs/22888090786
- Commit SHA: 6e8d7cf
- Artifact: phpunit-test-log; JUnit path: build/logs/junit.xml

---

## **Log Entry: 008**

### **1. Component Information**

- Component Name: Sprint 7 Template Picker & Export/Import Test Coverage
- Date: 2026-03-24
- Generated By: Xiaohan
- Reviewed By: Daniel

**Traceability**

- Branch: testing-ci-setup (local post-pull updates)
- Test / doc changes:
  - New: `tests/Unit/PBSGTemplateManagerSchemaTest.php`
  - New: `tests/Unit/PBSGTemplateExportImportSourceTest.php`
  - New: `tests/Integration/PBSGSprint7AjaxSmokeTest.php`
  - Modified: `tests/black-box-test-cases.md` (added TC-20 ~ TC-24; fixed "Librain" -> "Librarian")
  - Modified: `tests/regression-checklist.md` (added "Template Picker & Export/Import (Sprint 7)" section)

**Approach:** Added source-contract unit tests and integration smoke tests for Sprint 7 authoring features. Focused on schema/constants, token substitution contract, export/import package contract, AJAX guard/error paths, and manual black-box/regression coverage alignment with Sprint 7 checklist.

### **2. Black-Box Tests Performed**

1. **Template Manager Schema Contract (4 tests)**
   - Input: Inspect `class-pbsg-template-manager.php` constants and create-table source fragments.
   - Expected: Correct table constants (`TABLE`, `DB_VER`, `OPT_VER`), expected columns, `dbDelta` usage, schema version update, default seeded template metadata.
   - Result: PASS

2. **Template / Export-Import Source Contract (4 tests)**
   - Input: Inspect `class-pbsg-template-manager.php` and `class-pbsg-export-import.php` source contract strings.
   - Expected: Template token placeholders (`{{TUTORIAL_TITLE}}`, `{{AUTHOR_NAME}}`, `{{CURRENT_DATE}}`, `{{LIBRARY_CATALOG_URL}}`) present; export package keys (`pbsg_version`, `title`, `steps`, `attachments`, etc.) present; attachment token mapping contract (`att_<id>`) present; invalid import file message present.
   - Result: PASS

3. **Sprint 7 AJAX Smoke (8 tests)**
   - Input: Call `ajax_get_templates`, `ajax_save_as_template`, `ajax_create_from_template`, and `PBSG_Export_Import::handle_import` under WPStubs with capability/nonce/file permutations.
   - Expected: New hooks registered; nonce checks executed; forbidden access returns JSON 403; validation errors returned for missing template params / missing upload / invalid JSON package.
   - Result: PASS

4. **Black-box / Regression Documentation Alignment**
   - Input: Compare Sprint 7 checklist to existing manual test docs.
   - Expected: Add missing manual cases for template-picker selection/title validation and import error/draft behavior.
   - Result: DOC UPDATED

### **3. Verification Evidence**

- PHPUnit (new tests):
  - `./vendor/bin/phpunit tests/Unit/PBSGTemplateManagerSchemaTest.php` -> PASS (4 tests)
  - `./vendor/bin/phpunit tests/Unit/PBSGTemplateExportImportSourceTest.php` -> PASS (4 tests)
  - `./vendor/bin/phpunit tests/Integration/PBSGSprint7AjaxSmokeTest.php` -> PASS (8 tests)
- JS baseline sanity run:
  - `npm run test:js -- tests/js/admin-step-utils.test.js` -> PASS
- Notes:
  - Vendor/php deprecation warnings appear during test execution but do not affect pass/fail outcomes.

---

## **Log Entry: 009**

### **1. Component Information**

- Component Name: Merge main→develop — Test Fixes for RBAC Gates, Hook Count, and Upload Stubs
- Date: 2026-03-24
- Generated By: Enzo (Qi Xiang Phang)
- Reviewed By: Xiaohan (Reagan)

**Traceability**

- Commit SHA: dc9442c
- Branch: main → develop (merge conflict resolution)
- Changed Files (test-side only — no production code changes):
  - `tests/Unit/PBSGH5PAjaxTest.php` (RBAC capability stub)
  - `tests/Integration/PBSplitGuidePluginSmokeTest.php` (action count 25→29)
  - `tests/Integration/Helpers/WPStubs.php` (wp_max_upload_size + size_format stubs)

**Context:** This merge combined two non-overlapping feature sets:
- **develop:** RBAC roles, inline H5P quiz factory (multichoice/blanks/singlechoice), dirty-state tracking, resizable split panes, performance benchmarks, file uploads, guide settings, structured intro fields
- **main:** Template picker with save-as-template, branch/sub-tutorial system for wrong-answer remediation, export/import, cover image, accessibility labels

Conflicts were resolved across 5 production files by preserving both sides. The 5 CI failures below were all test-side gaps caused by tests not yet accounting for features from the opposite branch.

### **2. Failures Diagnosed and Fixed**

#### A. PBSGH5PAjaxTest — RBAC permission gate (3 failures)

- **Root cause:** develop added `current_user_can('view_h5p_contents')` to `ajax_list_h5p()` (line 1691 of `pb-split-guide.php`). The unit tests were written on main before this gate existed and did not stub the capability.
- **Symptom:** All 3 DB-level tests hit `'Insufficient permissions'` before reaching table-existence or query logic.

| Test | Pre-fix | Post-fix |
|------|---------|----------|
| `test_returns_error_when_h5p_table_does_not_exist` | FAIL — expected "H5P table not found", got "Insufficient permissions" | PASS |
| `test_returns_success_with_items_when_h5p_table_exists` | FAIL — `assertFalse is true` (wp_send_json_success never called) | PASS |
| `test_returns_empty_items_when_table_exists_but_no_rows` | FAIL — `Trying to access array offset on null` | PASS |
| `test_checks_referer_before_db_access` | PASS (unaffected) | PASS |

- **Fix:** Added `WPStubs::$returns['current_user_can'] = true;` in `setUp()`.
- **Rationale:** Test-side fix. The RBAC gate is correct production behavior from develop's security hardening.

#### B. PBSplitGuidePluginSmokeTest — Hook count mismatch (1 failure)

- **Root cause:** The merge added 4 new `add_action` calls from main's template picker system (lines 126–128: 3 AJAX handlers + line 131: admin_init for table creation). The expected count was still 25 from the previous merge.
- **Symptom:** `Expected 25 actions` but `actual size 29`.

| Test | Pre-fix | Post-fix |
|------|---------|----------|
| `test_total_hook_count_matches_expected` | FAIL — 29 ≠ 25 | PASS |

- **Fix:** Updated assertion from `assertCount(25, ...)` to `assertCount(29, ...)`.
- **Full constructor audit:** See `tests/logs/TEST-LOG-integration-SmokeTest-hookcount.md` for the complete 29-action, 4-filter line-by-line breakdown.

#### C. PBSplitGuidePluginSmokeTest — Missing WP function stubs (1 failure)

- **Root cause:** develop added file-upload support to `enqueue_admin_assets()`, which calls `wp_max_upload_size()` and `size_format()` (lines 1534–1535). Neither was stubbed in WPStubs.php.
- **Symptom:** `Call to undefined function wp_max_upload_size()` when the smoke test invokes `enqueue_admin_assets('post.php')`.

| Test | Pre-fix | Post-fix |
|------|---------|----------|
| `test_enqueue_admin_assets_loads_for_page_editor` | FAIL — fatal: undefined function | PASS |
| `test_enqueue_admin_assets_skips_non_editor_pages` | PASS (unaffected) | PASS |
| `test_enqueue_admin_assets_skips_non_page_post_type` | PASS (unaffected) | PASS |

- **Fix:** Added `wp_max_upload_size()` stub (returns 2097152 / 2 MB default, configurable via `WPStubs::$returns`) and `size_format()` stub (log-based unit conversion matching WP core behavior) to WPStubs.php.

### **3. Verification Evidence**

- Local Run: 335 tests, 833 assertions, 0 failures (PHPUnit 9.6.34, PHP 8.3, 0.313s, 16.00 MB)
- Test command: `lando phpunit --configuration phpunit.xml --testdox`
- CI Run: https://github.com/qixiang03/guide-on-the-side/actions/runs/23476477137
- Artifact: phpunit-test-log
- JUnit Path: build/logs/junit.xml
- Detailed per-failure logs: `tests/logs/TEST-LOG-unit-PBSGH5PAjaxTest.md`, `tests/logs/TEST-LOG-integration-SmokeTest-hookcount.md`, `tests/logs/TEST-LOG-integration-SmokeTest-enqueue.md`

---

## **Log Entry: 010**

### **1. Component Information**

- Component Name: Accessibility Dashboard — Custom Admin Color Schemes, Keyboard Shortcuts & ARIA Labels
- Date: 2026-03-29
- Generated By: Enzo (Qi Xiang Phang)
- Code Developer: Caleb
- Reviewed By: Xiaohan 

**Traceability**

- Commit SHA: 8a4b827
- Source Commits: 5f5b76b (feature, Caleb), b5847b3 (style cleanup, Caleb)
- Branch: develop
- Changed Files:
  - **New:** `accessibility-dashboard/class-pbsg-accessibility-dashboard.php` (622 lines — full dashboard class)
  - **New:** `accessibility-dashboard/assets/accessibility-dashboard.js` (121 lines — frontend toggle/persistence logic)
  - **New:** `accessibility-dashboard/assets/accessibility-dashboard-profile.js` (76 lines — profile page preview)
  - **New:** `accessibility-dashboard/styles/admin-colors-colorblind.css` (919 lines — colorblind-friendly admin theme)
  - **New:** `accessibility-dashboard/styles/admin-colors-upei.css` (915 lines — UPEI-branded admin theme)
  - **New:** `accessibility-dashboard/styles/profile.css` (186 lines — profile settings UI)
  - **New:** `accessibility-dashboard/README.md` (63 lines — feature documentation)
  - **Modified:** `assets/split-guide.js` (+34 lines — `tutorial-active` body class, ARIA label on tutorial iframe, custom keyboard shortcut handler via `window.aeShortcuts`)
  - **Modified:** `templates/split-guide-template.php` (+6 lines — ARIA landmark roles)
  - **Modified:** `pb-split-guide.php` (+1 line — require accessibility dashboard class)
  - **Modified:** `assets/analytics-dashboard.css` (-80 lines — removed hardcoded colors for theme compatibility)
  - **Modified:** `assets/analytics-dashboard.js` (+8 lines — empty title fallback, semantic button class)
  - **Modified:** `assets/split-guide.css` (-13 lines — removed hardcoded background/color values from menu, focus buttons, and step items)
  - **Modified:** `class-pbsg-analytics-dashboard.php` (-9 lines — replaced inline styles with semantic classes)

**Approach:** Added a full accessibility settings dashboard to the WordPress user profile page, allowing users to select custom admin color schemes (UPEI branded, colorblind-friendly), font families, font sizes, and keyboard shortcut bindings. A follow-up commit removed hardcoded color values from the split-guide and analytics dashboard CSS/JS so that all UI elements respect the active admin color scheme rather than overriding it.

### **2. Black-Box Tests Performed**

1. **Admin Color Scheme Registration**
   - Input/Action: Navigate to Users → Profile in WP Admin.
   - Expected: Two new color schemes appear: "UPEI Library" and "Colorblind Friendly", in addition to WordPress defaults.
   - Result: PASS

2. **Accessibility Profile Settings Section**
   - Input/Action: Scroll to "Accessibility Enhancer" section on the user profile page.
   - Expected: Settings fields render for: Font Family (dropdown), Font Size (range), Custom Keyboard Shortcuts (prev/next/focus quiz/focus tutorial), with live preview.
   - Result: PASS

3. **Profile Save & Persistence**
   - Input/Action: Change font to "UPEI Library Default", set font size to 18px, change shortcut keys, and save profile.
   - Expected: All settings persist via `update_user_meta()` and apply on next page load. Shortcuts saved as individual user meta keys (`ae_shortcut_prev`, `ae_shortcut_next`, etc.).
   - Result: PASS

4. **Custom Keyboard Shortcuts on Frontend Tutorial**
   - Input/Action: Open a split-guide tutorial page. Press the configured shortcut keys for prev/next/focus quiz/focus tutorial.
   - Expected: `window.aeShortcuts` object populated from `wp_localize_script()`. Keydown handler skips when focus is in INPUT/TEXTAREA/SELECT or contentEditable. Prev/Next buttons clicked programmatically. Focus toggles work.
   - Result: PASS

5. **ARIA Labels & Landmark Roles**
   - Input/Action: Inspect the split-guide template HTML output.
   - Expected: Tutorial iframe has `aria-label="Tutorial Frame"`. Template includes ARIA landmark attributes on main content areas.
   - Result: PASS

6. **Body Class for Tutorial Pages**
   - Input/Action: Load a split-guide tutorial page. Inspect `document.body.classList`.
   - Expected: `tutorial-active` class added immediately on script execution.
   - Result: PASS

7. **Theme Compatibility — Hardcoded Color Removal**
   - Input/Action: Switch between "Default", "UPEI Library", and "Colorblind Friendly" admin color schemes. View the split-guide tutorial, menu dropdown, focus buttons, and analytics dashboard.
   - Expected: No hardcoded `background`, `color`, or `:hover` background values override the active theme. Menu buttons, step items, focus buttons, and analytics export button all inherit theme colors.
   - Result: PASS

8. **Analytics Dashboard — Empty Title Fallback**
   - Input/Action: Create a tutorial with no title. Open the analytics dashboard comparison view.
   - Expected: Tutorial dropdown shows "(no title)" instead of an empty option.
   - Result: PASS

9. **Colorblind-Friendly Theme Contrast**
   - Input/Action: Activate "Colorblind Friendly" color scheme. Navigate across WP admin pages.
   - Expected: All UI elements meet WCAG AA contrast ratios. No reliance on color alone for state indication.
   - Result: PASS

### **3. Verification Evidence**

- Tested on: Pressbooks Local (Lando), PHP 8.3, WordPress 6.x
- Browser: Google Chrome (latest)
- Source commits: 5f5b76b (feat), b5847b3 (style)
- Verification method: Manual browser testing + DOM inspection via Chrome DevTools
- No automated tests added in this commit (frontend-only feature, tested manually)

---

## **Log Entry: 011**

### **1. Component Information**

- Component Name: Branch Sub-Tutorial Frontend Restoration — Cindy's Code Lost in Merge 8f14a70
- Date: 2026-03-29
- Generated By: Enzo (Qi Xiang Phang)
- Code Developer: Cindy (original branch code), Enzo (restoration)
- Reviewed By: Yang

**Traceability**

- Commit SHA: 1401a68
- Source Commit: 9d5e740 (restoration by Enzo), original code from 634d035 (Cindy's branch), images from 7482705
- Branch: develop
- Implementation Plan: `docs/PLAN-restore-cindy-branch-subtutorials.md`
- Changed Files:
  - **Modified:** `assets/split-guide.js` (+388/-17 lines — 10 branch functions, branch DOM refs, state variables, branch-aware `updatePassState`, mandatory branch blocking in `render()`, state clearing in `resetTutorialToStart()`, branch button event handlers, Vimeo/Dailymotion/TED embed URL parsers, removed pre-existing duplicate functions)
  - **Modified:** `templates/split-guide-template.php` (+40 lines — branch data assembly in PHP step enrichment loop, branch modal HTML with accessible dialog markup)
  - **Restored:** `assets/images/tutorial cover 1.png` (11,813 bytes)
  - **Restored:** `assets/images/tutorial cover 2.png` (8,618 bytes)
  - **Restored:** `assets/images/tutorial cover 3.png` (123,363 bytes)

**Context:** During the merge of main→develop at commit 8f14a70, conflict resolution favored the develop side for `split-guide.js` and `split-guide-template.php`, which dropped Cindy's entire branch sub-tutorial frontend system (~430 lines). The admin-side code (`admin-split-guide.js` branch picker UI, `split-guide.css` branch modal styles, `steps-normalizer.php` branch field validation) was NOT lost and remained intact on develop. This restoration extracted Cindy's exact code verbatim from commit 634d035 and inserted it into the current develop HEAD, alongside Enzo's additions (resize handle, structured intro, accessibility shortcuts) that didn't exist in Cindy's version.

### **2. Black-Box Tests Performed**

1. **Branch Modal DOM Elements Resolve**
   - Input/Action: Load a split-guide tutorial page with branch-configured steps. Run `document.getElementById()` for all 7 branch DOM refs (`pbsgBranchModal`, `pbsgBranchText`, `pbsgBranchOpen`, `pbsgBranchReturn`, `pbsgBranchComplete`, `pbsgBranchSkip`, `pbsgBranchClose`).
   - Expected: All 7 elements resolve (not null) when branch modal HTML is present in template.
   - Result: PASS

2. **Branch Detection — `hasBranch(step)`**
   - Input/Action: Inspect steps array via browser console. Steps 1 and 2 have `branch.mode` set to "optional" and "mandatory" respectively; Step 3 has "none".
   - Expected: `hasBranch()` returns true for Steps 1 and 2, false for Step 3.
   - Result: PASS

3. **Optional Branch Triggering (Step 1 — trigger after 1 wrong attempt)**
   - Input/Action: Answer the fill-in-the-blank quiz incorrectly once on Step 1.
   - Expected: Branch modal appears with title, intro text, "Start" button, "Skip" button, and Close (×) button. NEXT remains locked.
   - Result: PASS

4. **Optional Branch — Skip Button**
   - Input/Action: Click "Skip" on the optional branch modal.
   - Expected: Modal dismisses. Quiz resets to allow retry. NEXT remains locked until correct answer.
   - Result: PASS

5. **Optional Branch — Close Button**
   - Input/Action: Trigger optional branch again. Click the Close (×) button.
   - Expected: Modal dismisses. Same behavior as Skip.
   - Result: PASS

6. **Optional Branch — Correct Answer Clears Branch**
   - Input/Action: After triggering optional branch, answer the quiz correctly.
   - Expected: Branch UI resets (no modal, no banner). NEXT unlocks.
   - Result: PASS

7. **Mandatory Branch Triggering (Step 2 — trigger after 2 wrong attempts)**
   - Input/Action: Navigate to Step 2. Trigger mandatory branch (wrong answers reach threshold).
   - Expected: Mandatory branch modal appears with title "Need a mandatory helpful review?", intro text, and enforcement message "You can continue only after you finish this sub-tutorial and answer the main quiz correctly."
   - Result: PASS

8. **Mandatory Branch — No Skip, No Close**
   - Input/Action: Inspect the mandatory branch modal UI.
   - Expected: No "Skip" button visible. No Close (×) button visible. Only "Start" and "I Finished This Sub-Tutorial" buttons present.
   - Result: PASS (verified via zoomed screenshot)

9. **Mandatory Branch — "I Finished" Flow**
   - Input/Action: Click "I Finished This Sub-Tutorial" on the mandatory branch modal.
   - Expected: Modal dismisses. Branch marked as completed. NEXT remains locked (quiz not yet answered correctly).
   - Result: PASS

10. **Mandatory Branch — Dual Gate Unlock**
    - Input/Action: After completing mandatory branch, answer the quiz correctly ("bonjour").
    - Expected: NEXT unlocks only when BOTH conditions are met (branch completed AND quiz correct).
    - Result: PASS

11. **No Branch Step (Step 3 — branch mode "none")**
    - Input/Action: Navigate to Step 3. Answer incorrectly.
    - Expected: Standard wrong-answer behavior (0/1, Retry). No branch modal appears regardless of wrong answer count.
    - Result: PASS

12. **Branch Data Assembly in PHP**
    - Input/Action: Inspect `steps` array in browser console (`JSON.parse(document.querySelector('[data-steps]')...)`).
    - Expected: Each step contains `branch` object with `mode`, `trigger_attempts`, `title`, `intro`, and `tutorial` sub-object (type, url, file_url, mime).
    - Result: PASS

13. **Vimeo URL Embed Conversion**
    - Input/Action: Call `toEmbeddableUrl()` with `https://vimeo.com/123456789`, `https://vimeo.com/channels/staffpicks/987654`, `https://player.vimeo.com/video/111222333`.
    - Expected: All convert to `https://player.vimeo.com/video/{id}`.
    - Result: PASS

14. **Dailymotion URL Embed Conversion**
    - Input/Action: Call `toEmbeddableUrl()` with `https://www.dailymotion.com/video/x8abcde` and `https://dai.ly/x8abcde`.
    - Expected: Both convert to `https://www.dailymotion.com/embed/video/x8abcde`.
    - Result: PASS

15. **TED URL Embed Conversion**
    - Input/Action: Call `toEmbeddableUrl()` with `https://www.ted.com/talks/sir_ken_robinson_do_schools_kill_creativity` and `https://embed.ted.com/talks/already_embedded`.
    - Expected: First converts to `https://embed.ted.com/talks/sir_ken_robinson_do_schools_kill_creativity`. Second passes through unchanged.
    - Result: PASS

16. **Duplicate Function Cleanup**
    - Input/Action: Run `node -c split-guide.js` and grep for `getFinalGradePercent` and `hasIntroScreen`.
    - Expected: Zero syntax errors. Each function appears exactly once (pre-existing duplicates at former lines 1081/1090 removed).
    - Result: PASS

17. **No Regressions — Existing Features**
    - Input/Action: Verify menu dropdown, Focus Quiz/Tutorial buttons, structured intro screen, progress bar, keyboard shortcuts, and navigation (Prev/Next) all still function.
    - Expected: All existing features work as before. Resize handle correctly not rendered (gated behind `$user_resizable` PHP flag, not enabled on test tutorial).
    - Result: PASS

### **3. Verification Evidence**

- Tested on: Pressbooks Local (https://pressbooks.test), PHP 8.3, WordPress 6.x
- Browser: Google Chrome (latest) via Chrome  + manual DOM inspection
- JS syntax check: `node -c split-guide.js` — 0 errors
- Source commit: 9d5e740 (restoration from 634d035 verbatim)
- Implementation plan: `docs/PLAN-restore-cindy-branch-subtutorials.md` — all 10 insertion steps executed
- Verification checklist: 12/12 items from plan's verification section confirmed
- No automated tests added (frontend-only feature; admin branch picker already on develop)

---

## **Log Entry: 012**

### **1. Component Information**

- Component Name: pb-split-guide automated test gap closure (unit + integration smoke strategy + Jest)
- Date: 2026-03-30
- Generated By: Xiaohan
- Reviewed By: Yang

**Traceability**

- Branch: (current develop / feature branch as applicable)
- Test / infrastructure changes:
  - **Modified:** `tests/Integration/PBSplitGuidePluginSmokeTest.php` — replaced fragile exact hook counts with `test_constructor_registers_expected_plugin_hooks` (asserts required filter/action tags + minimum `admin_init` / `admin_menu` callbacks).
  - **Modified:** `tests/Integration/Helpers/WPStubs.php` — `WP_Error` / `is_wp_error`, upload constants, post/media helpers (`wp_insert_post`, `get_edit_post_link`, `media_handle_upload`, `get_attached_file`, `get_posts`, `get_permalink`, `wp_get_current_user`), sanitization helpers, optional `current_user_can_resolver` callable for per-capability tests.
  - **Modified:** `tests/Unit/Helpers/MockWpdb.php` — `delete()` for template row removal tests.
  - **New:** `tests/Unit/PBSGTemplateManagerTest.php` — `PBSG_Template_Manager` (`maybe_create_tables` no-op when version matches, CRUD-ish paths, `create_from_template` blank + missing template).
  - **New:** `tests/Unit/PBSGExportImportTest.php` — `PBSG_Export_Import::init`, export permission/missing post (`wp_die`), import permission/upload/invalid JSON/minimal successful package.
  - **New:** `tests/Unit/PBSGH5PFactoryTest.php` — invalid quiz types, `reverse()` round-trips via reflected `build_params`, `generate_title` via reflection.
  - **New:** `tests/Unit/PBSGPluginAjaxHandlersTest.php` — `ajax_get_templates`, `ajax_save_as_template`, `ajax_create_from_template`, `ajax_upload_file`, `ajax_create_h5p`, `ajax_get_h5p_content`, `ajax_list_tutorials` (guards + selected success paths).
  - **New:** `tests/Unit/PBSGAccessibilityEnhancerTest.php` — `Pressbooks_Accessibility_Enhancer::add_pdf_accessibility` / `add_epub_accessibility` via `ReflectionClass::newInstanceWithoutConstructor()`.
  - **New (Jest):** `tests/js/analytics-dashboard-compare-url.test.js` — compare-view `ids` query parsing (mirrors `analytics-dashboard.js` init).
  - **New (Jest):** `tests/js/split-guide-menu.test.js` — menu open/close DOM + `aria-expanded` (aligned with `split-guide.js` menu helpers).
  - **Docs:** `docs/TESTING_LOG.md`, `tests/black-box-test-cases.md`, `tests/regression-checklist.md` (this entry and manual TC alignment).

**Approach:** Close gaps called out in the pb-split-guide test plan: template manager, export/import, H5P factory logic without a live H5P core, extended plugin AJAX coverage, minimal a11y export-hook tests, and small Jest targets where the production files are large IIFEs. Smoke tests no longer depend on a fixed action/filter count when the constructor grows.

### **2. Black-Box Tests Performed**

1. **Manual regression (recommended after pull)**
   - Input: Run `tests/regression-checklist.md` including new Template Picker & Export/Import items (TC-14–TC-17) and existing Analytics/Certificate items.
   - Expected: No regressions in authoring, import/export, analytics, or tutorial play.
   - Result: _To be filled on next manual run_

2. **Automated — PHPUnit (`tests/Unit`)**
   - Input: `./vendor/bin/phpunit tests/Unit`
   - Expected: All unit tests pass (includes new PBSG\* test classes above).
   - Result: PASS (local; count varies with suite growth)

3. **Automated — Jest**
   - Input: `npm test`
   - Expected: All `tests/js/**/*.test.js` pass (including new compare-url and split-guide-menu suites).
   - Result: PASS (local)

4. **Known limitation — full `phpunit` suite**
   - Input: `./vendor/bin/phpunit` (entire project including `@runInSeparateProcess` smoke cases).
   - Note: Two tests in `PBSplitGuidePluginSmokeTest` (`test_save_meta_bails_during_autosave`, `test_activation_hook_registered_with_create_tables`) may error in subprocess runs when PHP deprecations from vendor/bootstrap are promoted to exceptions by PHPUnit. **Unit suite (`tests/Unit`) and the updated hook-tag smoke test pass in-process.** Track under CI/php.ini if subprocess strictness must be fixed.

### **3. Verification Evidence**

- Commands:
  - `./vendor/bin/phpunit tests/Unit`
  - `./vendor/bin/phpunit tests/Integration/PBSplitGuidePluginSmokeTest.php --filter test_constructor_registers_expected`
  - `npm test`
- JUnit (when run via project config): `build/logs/junit.xml` (path per `phpunit.xml`)

---

## **Log Entry: 013**

### **1. Component Information**

- Component Name: pb-split-guide — specific confirmation / navigation guard copy (user-visible dialogs)
- Date: 2026-03-30
- Generated By: Xiaohan
- Reviewed By: Yang

**Traceability**

- Production changes:
  - **Modified:** [web/app/plugins/pb-split-guide/pb-split-guide.php](web/app/plugins/pb-split-guide/pb-split-guide.php) — `wp_localize_script( 'pbsg_admin_js', 'PBSG_ADMIN', … )` extended with `postTitle` and `strings` (`confirmRemoveStep` with `%1$d` / `%2$s`, `untitledStep`, `leaveWithTitle` with `%s`, `leaveGeneric`); admin script handle version `0.7.0` → `0.7.1` (cache bust).
  - **Modified:** [web/app/plugins/pb-split-guide/assets/admin-split-guide.js](web/app/plugins/pb-split-guide/assets/admin-split-guide.js) — remove-step `confirm()` uses step index + title (truncated for dialog length); `beforeunload` message names the tutorial when `postTitle` is non-empty; helpers `pbsgAdminI18n()`, `truncateForConfirm()`.
  - **Modified:** [web/app/plugins/pb-split-guide/includes/class-pbsg-librarian-manager.php](web/app/plugins/pb-split-guide/includes/class-pbsg-librarian-manager.php) — `pbsgLibrarians` localized with `confirmDeactivateFmt` / `confirmReactivateFmt` (`%1$s` display name, `%2$s` login) plus fallbacks; script version `1.0.0` → `1.0.1`.
  - **Modified:** [web/app/plugins/pb-split-guide/templates/admin-manage-librarians.php](web/app/plugins/pb-split-guide/templates/admin-manage-librarians.php) — `data-display-name` / `data-user-login` on deactivate form; reactivate forms use class `pbsg-reactivate-form` and the same `data-*` attributes.
  - **Modified:** [web/app/plugins/pb-split-guide/assets/admin/admin-librarians.js](web/app/plugins/pb-split-guide/assets/admin/admin-librarians.js) — `formatUserConfirm()`; deactivate confirm reads template `data-*`; new submit handler for `form.pbsg-reactivate-form` with confirm before POST.

**Approach:** Replace generic “Remove this step?” / “this librarian” / static leave warnings with strings that state **which** step, **which** tutorial (when known), and **which** user (display name + login). Strings remain translatable in PHP; JS performs placeholder substitution. No new automated tests in this pass (browser `confirm()` / `beforeunload` are manual or E2E candidates).

### **2. Black-Box Tests Performed**

1. **Tutorial editor — remove step**
   - Input: Open a Split Guide page editor with multiple steps; click remove (×) on a step with a title and on a step with an empty title.
   - Expected: `confirm` text includes **Step N** and **title** (or translated “untitled” fallback); Cancel leaves steps unchanged; OK removes the correct row.
   - Result: _To be filled on next manual run_

2. **Tutorial editor — leave with unsaved changes**
   - Input: Change steps or intro fields without saving; attempt to close tab or navigate away.
   - Expected: Browser prompt mentions **tutorial title** when the post has a saved title; generic “this tutorial” copy when title is empty (e.g. new post).
   - Result: _To be filled on next manual run_

3. **Manage Librarians — deactivate**
   - Input: Open Manage → librarian profile (manage sub-action); submit **Deactivate Librarian**.
   - Expected: `confirm` names **display name** and **username** (login); Cancel aborts; OK proceeds.
   - Result: _To be filled on next manual run_

4. **Manage Librarians — reactivate**
   - Input: From deactivated list, click **Reactivate** for a former librarian.
   - Expected: `confirm` names that user; Cancel aborts; OK proceeds.
   - Result: _To be filled on next manual run_

### **3. Verification Evidence**

- Automated: existing PHPUnit / Jest suites unchanged by requirement (no new test files for dialog copy).
- Manual: run scenarios in §2 in Pressbooks admin (Chrome or equivalent).
- Related product spec: specific confirmations for delete step, deactivation, and parity for reactivation; plugin-wide inventory showed no additional `confirm()` calls beyond these paths at time of change.

---

## **Log Entry: 014**

### **1. Component Information**

- Component Name: Analytics Audit Fixes — Branch Quiz Exclusion, Draft Filtering, Device Detection, Normalizer Bug
- Date: 2026-04-06
- Generated By: Enzo (Qi Xiang Phang)
- Reviewed By: Xiaohan

**Traceability**

- Branch: `feature/sprint9-analytics-dashboard-fixes` → `main`
- Commit SHAs: 75bbbb1, 9692a77, 7ca9489, 020b62f, 77aa3a6, 9355ad5, e5bc7ac

- Production changes:
  - **Modified:** `class-pbsg-analytics.php` — Added `get_post_status()` guard in `handle_track_event()` returning 410 Gone for non-published tutorials; rewrote `detect_device()` to accept `$touch_points` parameter combining UA string with `navigator.maxTouchPoints` for iPadOS/Android tablet detection; updated caller to extract `touch_points` from event payload.
  - **Modified:** `includes/steps-normalizer.php` — Defined `$has_per_question_resource` variable (was undefined at line 203), restoring `per_question` branch resource mode so each sub-question can have its own right-panel iframe.
  - **Modified:** `assets/split-guide.js` — Exposed `window.pbsgInBranch()` read-only accessor for the internal `inBranch` navigation state.
  - **Modified:** `assets/split-guide-tracker.js` — Added branch quiz guards (skip `quiz_attempt`/`quiz_giveup` when `pbsgInBranch()` is true); added `touch_points: navigator.maxTouchPoints || 0` to `sendEvent()` and `sendBeaconEvent()` payloads; added `.then()` handler on fetch to detect 410 response and redirect to `/?pbsg-error=410`.
  - **Modified:** `templates/split-guide-template.php` — Wrapped tracker script enqueue and `wp_localize_script` in `get_post_status() === 'publish'` check.
  - **Modified:** `pb-split-guide.php` — Added `template_redirect` hook and `handle_error_page()` method rendering PBSG-410 error page (UPEI Design System styling, Close Page button with `window.close()` fallback, WCAG AA accessible).

- Test changes:
  - **Modified:** `tests/Integration/Helpers/WPStubs.php` — Added `get_post_status()`, `status_header()` stubs; fixed nullable parameter deprecation on `get_header()`/`get_footer()` stubs.
  - **Modified:** `tests/Unit/PBSGStepsNormalizerTest.php` — Added 2 tests: `test_per_question_branch_resource_mode_preserves_branch`, `test_per_question_branch_without_any_resources_strips_branch`.
  - **Modified:** `tests/Unit/PBSGAnalyticsEventIngestionTest.php` — Added 2 draft filtering tests and 12-case `@dataProvider deviceDetectionProvider` covering Chrome desktop, Safari macOS, iPadOS Safari/Chrome (touch=5), iPhone, Android phone, Android tablet, legacy iPad UA, MacBook trackpad (touch=1), Kindle Silk, iPod touch, and empty UA.

**Approach:** Full code review of the analytics pipeline (client → AJAX → DB → dashboard) identified 4 bugs and 1 missing feature. TDD for PHP changes; JS changes verified via `node -c` syntax checks. Defense-in-depth pattern for draft filtering (client-side conditional enqueue + server-side post_status guard). Two-signal device detection (UA string + touch capability hint).

### **2. Black-Box Tests Performed**

1. **Branch Quiz Exclusion**
   - Input: Navigate a tutorial with branch/remediation sub-questions. Answer a main quiz incorrectly to trigger a branch. Answer branch sub-questions.
   - Expected: Analytics dashboard shows only the 10 main quiz questions, not the branch sub-questions. `quiz_attempt` and `quiz_giveup` events are not sent during branch navigation.
   - Result: _To be verified on next manual run_

2. **Draft Tracking Prevention — Template Layer**
   - Input: Create a tutorial in draft status. Preview it in the browser. Check browser DevTools Network tab for `pbsg_track_event` requests.
   - Expected: No tracker JS loaded (`split-guide-tracker.js` not in page source). No analytics AJAX requests fired.
   - Result: _To be verified on next manual run_

3. **Draft Tracking Prevention — Server Layer**
   - Input: Using a REST client (curl/Postman), POST a `tutorial_view` event with a `tutorial_page_id` pointing to a draft post.
   - Expected: Server responds with HTTP 410 Gone and body `{"success":false,"data":"tutorial_unavailable"}`.
   - Result: _To be verified on next manual run_

4. **Mid-Session Tutorial Unpublish**
   - Input: Open a published tutorial in one browser tab. In another tab (admin), change the tutorial status to Draft and save. Return to the student tab and click Next.
   - Expected: The next analytics event triggers a 410 response. Student is redirected to `/?pbsg-error=410`. PBSG-410 error page renders with "Tutorial Unavailable" heading, "Close Page" button, and error code. Clicking "Close Page" attempts `window.close()`; if blocked, fallback text appears.
   - Result: _To be verified on next manual run_

5. **Device Detection — iPadOS Safari**
   - Input: Access a published tutorial from an iPad running iPadOS 13+ using Safari. Check analytics dashboard device breakdown after interaction.
   - Expected: Device logged as `tablet`, not `desktop`. Verified by checking `pbsg_daily_stats.device_type` in the database.
   - Result: _To be verified on next manual run_

6. **Device Detection — Android Tablet**
   - Input: Access a published tutorial from an Android tablet (e.g., Samsung Galaxy Tab) using Chrome. Check analytics dashboard device breakdown.
   - Expected: Device logged as `tablet`, not `mobile`.
   - Result: _To be verified on next manual run_

7. **Device Detection — Desktop and Phone Unchanged**
   - Input: Access a published tutorial from a desktop browser (Chrome/Safari/Firefox on Windows/macOS) and from an iPhone.
   - Expected: Desktop logged as `desktop`. iPhone logged as `mobile`. No regressions from prior behavior.
   - Result: _To be verified on next manual run_

8. **Per-Question Branch Resource Mode**
   - Input: In the admin editor, create a step with a branch set to `per_question` resource mode. Assign different URLs to each branch sub-question. Save the tutorial. Reload the editor.
   - Expected: Branch data preserved on save — resource mode remains `per_question`, each sub-question retains its assigned URL. Previously this data was silently stripped.
   - Result: _To be verified on next manual run_

9. **PBSG-410 Error Page Accessibility**
   - Input: Navigate to `/?pbsg-error=410`. Use keyboard only (Tab, Enter). Run screen reader.
   - Expected: Card has `role="alert"`. Button is focusable with visible focus ring (`outline: 2px solid #517E1B`). Warning icon has `aria-hidden="true"`. Close button has `aria-label`. Touch target meets 44x44px minimum.
   - Result: _To be verified on next manual run_

10. **Existing Analytics Unaffected**
    - Input: Complete a full tutorial (published) on a desktop browser. Check analytics dashboard — Overview, Tutorial Detail, Question Drill-Down, and Compare tabs.
    - Expected: All metrics display correctly. Date range and device filters apply to daily_stats views. Question stats show `all-time` badges. Benchmarks color-code correctly. No regressions.
    - Result: _To be verified on next manual run_

### **3. Verification Evidence**

- Automated: 430 tests, 1021 assertions (16 new). 2 pre-existing errors from vendor deprecation warnings (`apereo/phpcas`, `wp-browser`) — unrelated to this PR.
  - `vendor/bin/phpunit --configuration phpunit.xml --testdox`
- JS syntax: `node -c` passed on `split-guide.js` and `split-guide-tracker.js`
- CI Run: https://github.com/qixiang03/guide-on-the-side/actions/runs/24020137768
- Commit SHA: 622645d
- Manual: Run scenarios in §2 on Pressbooks (Lando), Chrome and Safari, desktop and tablet viewports.
- New unit tests:
  - `PBSGStepsNormalizerTest::test_per_question_branch_resource_mode_preserves_branch`
  - `PBSGStepsNormalizerTest::test_per_question_branch_without_any_resources_strips_branch`
  - `PBSGAnalyticsEventIngestionTest::test_rejects_events_for_draft_tutorials`
  - `PBSGAnalyticsEventIngestionTest::test_accepts_events_for_published_tutorials`
  - `PBSGAnalyticsEventIngestionTest::test_device_detection` (12 data provider cases)

---

## **Log Entry: 015**

### **1. Component Information**

- Component Name: PBSG_Certificate — `ajax_mark_completed` completion timestamp contract & unit test
- Date: 2026-04-06
- Generated By: Xiaohan
- Reviewed By: Yang

**Traceability**

- Production: [`web/app/plugins/pb-split-guide/includes/class-pbsg-certificate.php`](web/app/plugins/pb-split-guide/includes/class-pbsg-certificate.php) — `PBSG_Certificate::ajax_mark_completed()`
- Tests: [`tests/Unit/PBSGCertificateTest.php`](tests/Unit/PBSGCertificateTest.php)

**Contract (documented in code):** Each successful `ajax_mark_completed` call **always** writes the **latest** completion timestamp via `current_time('timestamp')` and `update_user_meta()`, then returns that value in `wp_send_json_success` as integer `completed_ts`. Prior stored values are **not** preserved when the learner completes again.

### **2. Black-Box / Unit Tests Performed**

1. **mark_completed — first completion (unchanged)**
   - Input: Logged-in user, valid nonce, valid split-guide `tutorial_id`, stubbed `current_time`.
   - Expected: `update_user_meta` called with `pbsg_completed_{id}` and the stubbed timestamp string; JSON success includes matching integer `completed_ts`.
   - Result: PASS — `test_mark_completed_stores_meta_and_returns_success`

2. **mark_completed — repeat completion overwrites meta**
   - Input: `user_meta_storage` already contains `pbsg_completed_42 => 999`; stub `current_time` to `'1234567890'`.
   - Expected: `update_user_meta` still runs and stores `'1234567890'`; success payload `completed_ts` is `1234567890` (not `999`).
   - Result: PASS — `test_mark_completed_always_stores_latest_timestamp`

3. **Regression note — superseded expectation**
   - Previously a unit test asserted that an existing completion meta would cause the handler to return the **old** timestamp without overwriting. That contradicted the production rule above. The test was **replaced** to match “always latest timestamp.”

### **3. Verification Evidence**

- Command: `lando phpunit --configuration phpunit.xml tests/Unit/PBSGCertificateTest.php --testdox`
- Result: 11 tests, 34 assertions, 0 failures (PHPUnit 9.6.34)

---

## **Log Entry: 016**

### **1. Component Information**

- Component Name: H5P Ownership, Template Duplication, Inline Name Editing + Benchmark Sliders + CSV Export Fix
- Date: 2026-04-09
- Generated By: Enzo
- Reviewed By: Xiaohan

**Traceability**

- Production:
  - [`web/app/plugins/pb-split-guide/pb-split-guide.php`](web/app/plugins/pb-split-guide/pb-split-guide.php) — `ajax_list_h5p()`, `ajax_rename_h5p()`, `ajax_duplicate_h5p()`, `save_meta()`, `wp_localize_script` additions
  - [`web/app/plugins/pb-split-guide/includes/class-pbsg-h5p-usage-map.php`](web/app/plugins/pb-split-guide/includes/class-pbsg-h5p-usage-map.php) — `PBSG_H5P_Usage_Map` (new)
  - [`web/app/plugins/pb-split-guide/includes/class-pbsg-h5p-factory.php`](web/app/plugins/pb-split-guide/includes/class-pbsg-h5p-factory.php) — `generate_title()` and `get_h5p_core()` visibility changed to public
  - [`web/app/plugins/pb-split-guide/assets/admin-split-guide.js`](web/app/plugins/pb-split-guide/assets/admin-split-guide.js) — card rendering, lock/unlock, ownership warning, picker redesign, template duplication, metadata pre-loading
  - [`web/app/plugins/pb-split-guide/assets/admin/admin-split-guide.css`](web/app/plugins/pb-split-guide/assets/admin/admin-split-guide.css) — lock/unlock states, warning banner, picker badges
  - [`web/app/plugins/pb-split-guide/class-pbsg-analytics.php`](web/app/plugins/pb-split-guide/class-pbsg-analytics.php) — compare CSV dynamic tutorial headers
- Tests:
  - [`tests/Unit/PBSGH5PUsageMapTest.php`](tests/Unit/PBSGH5PUsageMapTest.php) (new — 6 tests)
  - [`tests/Unit/PBSGH5POwnershipTest.php`](tests/Unit/PBSGH5POwnershipTest.php) (new — 6 tests)
  - [`tests/Unit/PBSGH5PAjaxTest.php`](tests/Unit/PBSGH5PAjaxTest.php) (updated — 1 new test, 3 updated)
  - [`tests/Unit/PBSGAnalyticsCsvExportTest.php`](tests/Unit/PBSGAnalyticsCsvExportTest.php) (fixed — `test_compare_csv_source_has_metric_column`)

**Branch:** `feature/sprint9-analytics-dashboard-fixes` → merged to `main`

### **2. Black-Box / Unit Tests Performed**

#### H5P Usage Map (6 tests)

1. **Builds map from postmeta when transient is cold**
   - Input: No cached transient, two tutorials with step JSON referencing H5P IDs 42 and 87.
   - Expected: Map built as `{42: [101, 205], 87: [101]}`, `h5p_id=0` excluded, `set_transient` called.
   - Result: PASS — `test_builds_map_from_postmeta_when_transient_cold`

2. **Returns cached transient when warm**
   - Input: Pre-populated transient with usage map.
   - Expected: Returns cached map, no DB queries executed.
   - Result: PASS — `test_returns_cached_transient_when_warm`

3. **Count returns tutorial count for H5P ID**
   - Input: Cached map `{42: [101, 205, 310], 87: [101]}`.
   - Expected: `count(42)=3`, `count(87)=1`, `count(999)=0`.
   - Result: PASS — `test_count_returns_tutorial_count_for_h5p_id`

4. **Invalidate deletes transient**
   - Input: Call `invalidate()`.
   - Expected: `delete_transient('pbsg_h5p_usage_map')` called.
   - Result: PASS — `test_invalidate_deletes_transient`

5. **Handles empty postmeta**
   - Input: No `_pbsg_steps_json` rows in postmeta.
   - Expected: Returns empty map `[]`.
   - Result: PASS — `test_handles_empty_postmeta`

6. **Handles malformed JSON in postmeta**
   - Input: One row with invalid JSON, one valid row with H5P #42.
   - Expected: Invalid row skipped, map returns `{42: [205]}`.
   - Result: PASS — `test_handles_malformed_json_in_postmeta`

#### H5P Ownership Endpoints (6 tests)

7. **Rename succeeds for owner**
   - Input: Current user ID 5 matches H5P owner, title "New Quiz Name".
   - Expected: `wp_send_json_success` called, `$wpdb->update` sets title and ID correctly.
   - Result: PASS — `test_rename_succeeds_for_owner`

8. **Rename rejects non-owner**
   - Input: Current user ID 5, H5P owned by user 99.
   - Expected: `wp_send_json_error` with message containing "owner".
   - Result: PASS — `test_rename_rejects_non_owner`

9. **Rename rejects empty title**
   - Input: Whitespace-only title `"   "`.
   - Expected: `wp_send_json_error` called.
   - Result: PASS — `test_rename_rejects_empty_title`

10. **Rename checks referer**
    - Input: Valid rename request.
    - Expected: `check_ajax_referer` called with action `pbsg_h5p_picker`.
    - Result: PASS — `test_rename_checks_referer`

11. **Duplicate returns error for missing source**
    - Input: H5P ID 999, source not found in DB (`get_row` returns null).
    - Expected: `wp_send_json_error` with message containing "not found".
    - Result: PASS — `test_duplicate_returns_error_for_missing_source`

12. **Duplicate checks referer and capability**
    - Input: Valid duplicate request.
    - Expected: `check_ajax_referer` called with action `pbsg_h5p_picker`.
    - Result: PASS — `test_duplicate_checks_referer_and_capability`

#### Enriched H5P List (1 new test)

13. **List H5P includes ownership and usage data**
    - Input: Two H5P items (user 5 owns #42, user 3 owns #87), current user is 5, usage map cached.
    - Expected: Item #42 has `is_owner=true`, `usage_count=3`, `author_name='Cindy Guo'`. Item #87 has `is_owner=false`, `usage_count=1`, `author_name='Unknown user'`.
    - Result: PASS — `test_list_h5p_includes_ownership_and_usage_data`

#### CSV Export Fix (1 test updated)

14. **Compare CSV source has metric column (fixed)**
    - Root cause: Commit `107c5bd` changed compare CSV from hardcoded `'Tutorial 1', 'Tutorial 2', 'Tutorial 3'` headers to dynamic headers using `$t['name']`. Test still asserted old hardcoded strings.
    - Fix: Updated assertion to check for `$t['name']` instead of hardcoded tutorial column names.
    - Result: PASS — `test_compare_csv_source_has_metric_column`

### **3. Verification Evidence**

- CI Run: https://github.com/qixiang03/guide-on-the-side/actions/runs/24220746721
- Commit SHA: `63d2bec`
- Command: `vendor/bin/phpunit --configuration phpunit.xml --testdox`
- Result: 443 tests, 1048 assertions, 0 failures
- New tests added: 13 (6 usage map + 6 ownership + 1 enriched list)
- Tests updated: 4 (3 existing AJAX tests for new response shape + 1 CSV export fix)

---

## **Log Entry: 017**

### **1. Component Information**

- Component Name: Tutorial Intro Layout, Branch Sub-Quiz H5P Conversion, Quiz Typography Hierarchy Refresh, H5P Edit Bug Fix
- Date: 2026-04-12
- Generated By: Enzo
- Reviewed By: Xiaohan

**Traceability**

- Production:
  - [`web/app/plugins/pb-split-guide/templates/split-guide-template.php`](web/app/plugins/pb-split-guide/templates/split-guide-template.php) — two-column intro layout, "TUTORIAL" eyebrow + title, `pbsg-intro-card--no-cover` modifier, "Generate Certificate" / "Generate" button class upgrade to `pbsg-btn-primary`
  - [`web/app/plugins/pb-split-guide/assets/split-guide.css`](web/app/plugins/pb-split-guide/assets/split-guide.css) — intro flexbox two-column rules, `--pbsg-primary` updated to `#8C2004`, all button harmonization (MENU, FOCUS QUIZ, PREV, NEXT, Start, btn-outline, btn-primary, Learn More), cert modal/input refinement, summary card typography, dead branch inline CSS deleted, new `.pbsg-branch-h5p-iframe` / `.pbsg-branch-error` rules
  - [`web/app/plugins/pb-split-guide/assets/split-guide.js`](web/app/plugins/pb-split-guide/assets/split-guide.js) — `getH5PStyleCSS()` extracted for shared injection, `injectH5PStyle()` for branch iframes, `renderInlineBranchQuestion()` rewritten as H5P iframe loader, `attachBranchH5PxAPI()` added, `bindInlineBranchCheck()` deleted, page progress denominator changed to `totalPages`
  - [`web/app/plugins/pb-split-guide/pb-split-guide.php`](web/app/plugins/pb-split-guide/pb-split-guide.php) — `branch_question_to_quiz()`, `get_h5p_library_name()` helpers, `save_meta()` branch H5P loop with type-change defense
  - [`web/app/plugins/pb-split-guide/includes/class-pbsg-h5p-factory.php`](web/app/plugins/pb-split-guide/includes/class-pbsg-h5p-factory.php) — `get_library_for_type()` public accessor
  - [`web/app/plugins/pb-split-guide/includes/steps-normalizer.php`](web/app/plugins/pb-split-guide/includes/steps-normalizer.php) — `_editing_h5p` flag preservation through whitelist
  - [`web/app/plugins/pb-split-guide/assets/admin-split-guide.js`](web/app/plugins/pb-split-guide/assets/admin-split-guide.js) — `syncBranchQuestion()` sets `_editing_h5p` flag
- Tests:
  - [`tests/Unit/PBSGBranchH5PTest.php`](tests/Unit/PBSGBranchH5PTest.php) (new — 6 tests)

**Branch:** `feature/intro-layout-redesign` → merged to `main`

### **2. Unit Tests Performed**

#### Branch H5P Conversion Helpers (6 tests)

1. **branch_question_to_quiz — multichoice translates correctly**
   - Input: Branch question with type `multichoice`, question text, 3 answers (1 correct, 2 incorrect).
   - Expected: Returns quiz schema with `type='multichoice'`, question preserved, answers array with correct `text` and `correct` flags.
   - Result: PASS — `test_branch_multichoice_translates_correctly`

2. **branch_question_to_quiz — singlechoice translates correctly**
   - Input: Branch question with type `singlechoice`, question text, correct answer, 2 wrong answers.
   - Expected: Returns quiz schema with `type='singlechoice'`, `correct_answer` and `wrong_answers` preserved.
   - Result: PASS — `test_branch_singlechoice_translates_correctly`

3. **branch_question_to_quiz — blanks translates correctly**
   - Input: Branch question with type `blanks`, sentence with `*Ottawa*`, `case_sensitive=true`, `accept_typos=false`.
   - Expected: Returns quiz schema with `type='blanks'`, sentence preserved, boolean flags preserved.
   - Result: PASS — `test_branch_blanks_translates_correctly`

4. **branch_question_to_quiz — unsupported type returns null**
   - Input: Branch questions with type `dragndrop`, empty string, and missing type key.
   - Expected: All three return `null` (unsupported types are skipped by the H5P creation loop).
   - Result: PASS — `test_branch_unsupported_type_returns_null`

5. **get_library_for_type — known types**
   - Input: `multichoice`, `singlechoice`, `blanks`.
   - Expected: `H5P.MultiChoice`, `H5P.MultiChoice`, `H5P.Blanks` respectively.
   - Result: PASS — `test_get_library_for_known_types`

6. **get_library_for_type — unknown type returns null**
   - Input: `dragndrop`, empty string.
   - Expected: Both return `null`.
   - Result: PASS — `test_get_library_for_unknown_type_returns_null`

#### Bug Fix Verification

7. **_editing_h5p flag preserved through normalizer**
   - Root cause: `PBSG_Steps_Normalizer::normalize()` uses a whitelist pattern that stripped `_editing_h5p`, so `save_meta()` never reached the `PBSG_H5P_Factory::update()` path. Quiz edits were silently discarded.
   - Fix: Added `_editing_h5p` to the normalizer's output when present in input. The flag is transient — `save_meta()` strips it with `unset()` after use, so it never persists to the database.
   - Verification: Manual test — edit H5P inline, click Update, reload page → changes persist.

### **3. Verification Evidence**

- CI Run: https://github.com/qixiang03/guide-on-the-side/actions/runs/24301357605
- Commit SHA: `884a9ea`
- Artifact: phpunit-test-log (Actions run → Artifacts)
- JUnit path: `build/logs/junit.xml`
- Command: `vendor/bin/phpunit --configuration phpunit.xml --testdox`
- Result: 451 tests, 1082 assertions, 0 failures
- New tests added: 6 (4 branch_question_to_quiz + 2 get_library_for_type)
- Files changed: 13 files, +1188 / -485

---

## **Log Entry: 018**

### **1. Component Information**

- Component Name: Network Admin Menu Visibility Fix + Admin Guide + Dev Setup Guide Update
- Date: 2026-04-13
- Generated By: Enzo
- Reviewed By: Xiaohan

**Traceability**

- Production:
  - [`web/app/plugins/pb-split-guide/pb-split-guide.php`](web/app/plugins/pb-split-guide/pb-split-guide.php) — added `network_admin_menu` hooks for My Tutorials, Guide Settings, and Template Picker so all plugin menus appear in both Site Admin and Network Admin
- Documentation:
  - [`docs/ADMIN-GUIDE.md`](docs/ADMIN-GUIDE.md) (new) — Administrator Installation & Configuration Guide for client handoff (local testing via Lando, production deployment, plugin settings, librarian management, analytics, H5P, export/import, accessibility)
  - [`docs/DEV-SETUP-GUIDE.md`](docs/DEV-SETUP-GUIDE.md) (rewritten) — updated from outdated Jan 2026 Docker Compose guide to current Lando-based setup with correct domain (`pressbooks.test`), services, testing, XDebug, CI/CD, troubleshooting

**Branch:** `feature/sprint9-analytics-dashboard-fixes`

### **2. Bug Fix — Network Admin Menu Visibility**

**Root Cause:**
- `register_admin_menu()` (My Tutorials), `register_guide_settings_page()` (Guide Settings), and `register_template_picker_page()` (Template Picker) were only hooked to `admin_menu`, which registers menus in Site Admin only.
- The `network_admin_menu` hook was missing, so these items did not appear in the Network Admin dashboard.
- Other plugin components (Manage Librarians, Tutorial Analytics) already used both hooks and appeared correctly.

**Fix:**
- Added `add_action('network_admin_menu', ...)` alongside the existing `admin_menu` hooks for all three menu registrations.
- Access remains capability-gated: Guide Settings requires `manage_options`, Template Picker requires `edit_pages`, My Tutorials requires `read`.

**Verification:**
- Manual: Reloaded Network Admin → confirmed My Tutorials, Guide Settings, and Template Picker now appear in sidebar alongside existing Manage Librarians and Tutorial Analytics items.

### **3. Verification Evidence**

- CI Run: https://github.com/qixiang03/guide-on-the-side/actions/runs/24371161243
- Commit SHA: `3b500df`
- Artifact: phpunit-test-log (Actions run → Artifacts)
- JUnit path: `build/logs/junit.xml`
- Command: `vendor/bin/phpunit --configuration phpunit.xml --testdox`
- New tests added: 0 (menu hook registration — no unit test coverage for WP hook wiring)
- Documentation: 2 files (1 new, 1 rewritten)

---

## **Log Entry: 019**

### **1. Component Information**

- Component Name: Nav Wrap + Summary Redesign + Toolbar/Dropdown Polish + Google Docs Viewer Fix + composer.lock + CI/CD Doc
- Date: 2026-04-19
- Generated By: Enzo
- Reviewed By:
  _Peer review must be conducted by someone other than the developer._

**Traceability**

- Production (UI):
  - [`web/app/plugins/pb-split-guide/assets/split-guide.css`](web/app/plugins/pb-split-guide/assets/split-guide.css) — removed two conflicting `.pbsg-nav` grid rules; added CSS container-query tiers (480 / 300 / 180 px) on `.pbsg-left`; new eyebrow + Lusitana title styles; summary-card redesign; dropdown state slot styles
  - [`web/app/plugins/pb-split-guide/templates/split-guide-template.php`](web/app/plugins/pb-split-guide/templates/split-guide-template.php) — flattened toolbar markup to a 3-child flex row with title zone; dual-label nav spans; summary card rebuilt to mirror intro card structure; Steps dropdown reorganized with sticky mini-header
  - [`web/app/plugins/pb-split-guide/assets/split-guide.js`](web/app/plugins/pb-split-guide/assets/split-guide.js) — `render()` updates the eyebrow alongside the title; `updateMenuState()` tracks visited steps in an in-memory `Set<number>` and toggles overflow fade
  - [`web/app/plugins/pb-split-guide/includes/class-pbsg-icons.php`](web/app/plugins/pb-split-guide/includes/class-pbsg-icons.php) — added `maximize` four-corner-brackets icon entry for the collapsed Focus Quiz button
- Production (embed):
  - [`web/app/plugins/pb-split-guide/includes/class-pbsg-embed-check.php`](web/app/plugins/pb-split-guide/includes/class-pbsg-embed-check.php) — swapped `/gview` → `/viewerng/viewer` endpoint so Office-file (DOCX/XLSX/PPTX/CSV) previews render on the staging server
  - [`web/app/plugins/pb-split-guide/templates/split-guide-template.php`](web/app/plugins/pb-split-guide/templates/split-guide-template.php) — same endpoint swap on the non-embeddable-document-URL branch
- Tests:
  - [`tests/Unit/PBSGEmbedCheckTest.php`](tests/Unit/PBSGEmbedCheckTest.php) — updated 6 assertions to expect the new `/viewerng/viewer` endpoint
- Dependencies:
  - [`.gitignore`](.gitignore) — removed `composer.lock` from ignore list
  - [`composer.lock`](composer.lock) (newly committed) — pins 221 root-project dependencies (WordPress 6.9.4, Pressbooks `dev-dev`, PHPUnit 9.6.34, `roave/security-advisories` at `b0b156e`)
  - [`web/app/plugins/pb-split-guide/composer.lock`](web/app/plugins/pb-split-guide/composer.lock) (newly committed) — pins TCPDF 6.11.x
- Documentation:
  - [`docs/TECHNICAL-MANUAL.md`](docs/TECHNICAL-MANUAL.md) — added **§ 10. CI/CD & Maintenance** (GitHub Actions Node runtime deprecations, `composer.lock` rationale, rolling-advisory handling, deliberate dependency refresh, intentional CI non-features)

**Branch:** `feature/nav-summary-permissions-polish`

### **2. Changes Summary**

**UI Polish — 2026-04-16 design language extended across left pane**
- Fixed nav-button overlap at narrow pane widths: replaced the grid-based `.pbsg-nav` rule with a flexbox + container-query wrap so `Page X of Y` / `Correct/Attempted: X/Y` no longer clip behind Prev/Next when the user drags the split divider.
- Redesigned summary/completion card to match the intro card: green uppercase eyebrow → Lusitana serif title → description → objectives list (bounded `max-height: 220px` with overflow fade) → meta row → actions.
- Rebuilt top toolbar as a flat 3-child flex row (`[Menu ▼] [Title zone] [Focus Quiz]`) with 4 responsive tiers at 480 / 300 / 180 px; narrowest tier collapses Focus button to `maximize` icon only.
- Rebuilt Steps dropdown with sticky mini-header, scrollable list with `is-current` / `is-visited` state classes, and bottom fade when overflowing. Visited tracking is in-memory only — no persistence, PIPEDA-compliant.

**Embed Fix — Google Docs Viewer endpoint**
- Root cause: the legacy `/gview` endpoint has been quietly deprecated by Google and returns "No preview available" on staging even for publicly-reachable URLs that work with `/viewerng/viewer`.
- Verified via manual test: the user constructed `https://docs.google.com/viewerng/viewer?url=<staging docx>&embedded=true` manually and confirmed it renders, proving the URL format and server reachability were correct while `/gview` was at fault.
- Fix: two-line endpoint swap, plus 6 test-assertion updates.

**CI/CD Reproducibility**
- Unignored and committed both `composer.lock` files. CI was intermittently failing because `roave/security-advisories: dev-latest` is a rolling deny-list: every `composer install` without a lockfile pulled upstream's latest advisory set, which at one point conflicted with every currently-published PHPUnit 8.5 / 9.6 / 10.5 patch.
- Committing the lockfile pins Roave to the SHA (`b0b156e`) and PHPUnit to `9.6.34`, making future CI runs deterministic.

**Handoff Documentation**
- Added § 10 to `TECHNICAL-MANUAL.md` so the next maintainer knows: how to bump Node-runtime-deprecated action pins, why `composer.lock` is committed, how to respond when `composer update` hits a rolling advisory conflict, how to deliberately refresh dependencies, and what is intentionally excluded from CI (lint, deploy, coverage).

### **3. Verification Evidence**

- CI Run: https://github.com/qixiang03/guide-on-the-side/actions/runs/24623078733
- Commit SHA: `fb68a65`
- Artifact: phpunit-test-log (Actions run → Artifacts)
- JUnit path: `build/logs/junit.xml`
- Command: `vendor/bin/phpunit --configuration phpunit.xml --testdox`
- Local re-run: `lando phpunit --filter 'viewer_url' --testdox` → 8 tests / 13 assertions / PASS (viewer URL endpoint swap regression suite)
- New tests added: 0 (6 existing `viewer_url` assertions updated; no new behavior that needed new test coverage)
- Documentation: 1 file modified (§ 10 CI/CD & Maintenance added to TECHNICAL-MANUAL.md)

### **4. Black-Box Tests Performed (Manual)**

1. **Narrow-pane nav wrap**
   - Input/Action: Open a tutorial with ≥ 2 steps, drag the split divider to < 400 px wide.
   - Expected: Prev/Next buttons reflow below the progress indicator; no overlap; progress text remains fully visible.
   - Actual: PASS
2. **Summary card alignment**
   - Input/Action: Complete a tutorial with > 5 questions; land on the summary page.
   - Expected: Lusitana title + green eyebrow + bounded-height objectives list with overflow fade; visual alignment with intro card.
   - Actual: PASS
3. **Toolbar responsive tiers**
   - Input/Action: Resize left pane through 480 / 300 / 180 px breakpoints.
   - Expected: Toolbar reflows per tier; at narrowest, Focus Quiz button collapses to `maximize` icon only with `aria-label`.
   - Actual: PASS
4. **Steps dropdown state tracking**
   - Input/Action: Open Steps dropdown, navigate forward through steps, re-open dropdown.
   - Expected: Previous steps show `is-visited`; current step shows `is-current`; bottom fade appears when list overflows container.
   - Actual: PASS
5. **Google Docs Viewer DOCX render on staging**
   - Input/Action: Open a tutorial step whose right-pane embed is a `.docx` uploaded to the staging media library.
   - Expected: Document renders in-frame via `/viewerng/viewer`.
   - Actual: PASS (verified via manual URL assembly matching the working format prior to code fix)
6. **CI reproducibility**
   - Input/Action: Push branch to remote; trigger CI workflow.
   - Expected: `composer install` succeeds against the committed lockfile; PHPUnit reports all tests passing; JUnit artifact uploaded.
   - Actual: PASS (CI run 24623078733)
